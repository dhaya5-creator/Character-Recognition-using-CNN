# Character-Recognition-using-CNN
This project implements a Character Recognition System using a Convolutional Neural Network (CNN). The system is designed to recognize handwritten characters from input images by automatically learning important features such as edges, shapes, and patterns.
